import { describe, test, expect } from 'vitest'

describe.skip('Interview E2E (requires LLM + GitHub)', () => {
  test('ideateStart.REQ-010: full interview loop with real agent', () => {
    // Requires: LLM provider, GitHub API, interactive terminal
  })
})
